import type { InterviewQuestion } from '@/types/interviewQuestion';

// System Design & Coding Questions (Questions 26-50)
export const INTERVIEW_QUESTIONS_PART2: InterviewQuestion[] = [
  {
    id: 'q026',
    question: 'Design a recommendation system for an e-commerce platform.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Amazon',
    topics: ['Recommendation Systems', 'System Design', 'Scalability'],
    answer: 'Components: 1) Data collection: user behavior, product catalog, 2) Feature engineering: user features, item features, context, 3) Models: collaborative filtering (user-user, item-item), content-based, hybrid, 4) Serving: real-time inference, caching, A/B testing, 5) Evaluation: CTR, conversion rate, diversity. Architecture: offline training pipeline, online serving with low latency (<100ms), feedback loop for continuous learning.',
    hints: ['Consider both collaborative and content-based approaches', 'Think about real-time vs batch processing'],
    followUpQuestions: ['How do you handle cold start problem?', 'How do you ensure diversity in recommendations?'],
    timeToSolve: '30-45 minutes',
  },
  {
    id: 'q027',
    question: 'Design a real-time fraud detection system.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Stripe',
    topics: ['Fraud Detection', 'Real-time Systems', 'ML in Production'],
    answer: 'Architecture: 1) Data ingestion: streaming (Kafka), 2) Feature store: real-time features (transaction history, device fingerprint), 3) Model: ensemble (rules + ML), 4) Inference: <50ms latency, 5) Feedback loop: label transactions, retrain. Features: transaction amount, location, time, velocity, device info. Models: gradient boosting, neural networks. Handle: false positives (manual review queue), model drift (monitoring), scalability (horizontal scaling).',
    hints: ['Think about latency requirements', 'Consider both rule-based and ML approaches'],
    followUpQuestions: ['How do you handle concept drift?', 'What metrics would you track?'],
    timeToSolve: '30-45 minutes',
  },
  {
    id: 'q028',
    question: 'Implement a function to calculate cosine similarity between two vectors.',
    type: 'Coding',
    difficulty: 'Easy',
    company: 'Meta',
    topics: ['Embeddings', 'Similarity', 'Python'],
    answer: 'def cosine_similarity(a, b):\n    dot_product = sum(x * y for x, y in zip(a, b))\n    norm_a = sum(x ** 2 for x in a) ** 0.5\n    norm_b = sum(x ** 2 for x in b) ** 0.5\n    return dot_product / (norm_a * norm_b)\n\n# Or using numpy:\nimport numpy as np\ndef cosine_similarity(a, b):\n    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))',
    hints: ['Remember the formula: dot(a,b) / (||a|| * ||b||)', 'Consider edge cases like zero vectors'],
    followUpQuestions: ['What if one vector is all zeros?', 'How would you optimize for large batches?'],
    timeToSolve: '10-15 minutes',
  },
  {
    id: 'q029',
    question: 'Implement softmax function with numerical stability.',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'OpenAI',
    topics: ['Neural Networks', 'Numerical Stability', 'Python'],
    answer: 'def softmax(x):\n    # Subtract max for numerical stability\n    exp_x = np.exp(x - np.max(x))\n    return exp_x / np.sum(exp_x)\n\n# For batches:\ndef softmax_batch(x):\n    exp_x = np.exp(x - np.max(x, axis=-1, keepdims=True))\n    return exp_x / np.sum(exp_x, axis=-1, keepdims=True)',
    hints: ['Think about overflow with large numbers', 'Subtracting max prevents overflow'],
    followUpQuestions: ['Why does subtracting max help?', 'What is log-softmax and why use it?'],
    timeToSolve: '15 minutes',
  },
  {
    id: 'q030',
    question: 'Implement k-means clustering from scratch.',
    type: 'Coding',
    difficulty: 'Hard',
    company: 'Google',
    topics: ['Clustering', 'Algorithms', 'Python'],
    answer: 'def kmeans(X, k, max_iters=100):\n    # Initialize centroids randomly\n    centroids = X[np.random.choice(len(X), k, replace=False)]\n    \n    for _ in range(max_iters):\n        # Assign points to nearest centroid\n        distances = np.sqrt(((X - centroids[:, np.newaxis])**2).sum(axis=2))\n        labels = np.argmin(distances, axis=0)\n        \n        # Update centroids\n        new_centroids = np.array([X[labels == i].mean(axis=0) for i in range(k)])\n        \n        # Check convergence\n        if np.allclose(centroids, new_centroids):\n            break\n        centroids = new_centroids\n    \n    return labels, centroids',
    hints: ['Think about the two main steps: assignment and update', 'Consider convergence criteria'],
    followUpQuestions: ['How do you choose k?', 'What are limitations of k-means?'],
    timeToSolve: '30 minutes',
  },
  {
    id: 'q031',
    question: 'Design a search ranking system for a search engine.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Google',
    topics: ['Information Retrieval', 'Ranking', 'System Design'],
    answer: 'Components: 1) Query understanding: intent classification, entity extraction, 2) Candidate generation: inverted index, semantic search, 3) Ranking: learning to rank (LambdaMART, neural rankers), 4) Features: relevance, quality, freshness, personalization, 5) Serving: caching, latency optimization. Architecture: multi-stage (retrieval → ranking → re-ranking). Evaluation: NDCG, MRR, click-through rate. Challenges: query understanding, relevance vs diversity, personalization.',
    hints: ['Think about multi-stage ranking', 'Consider both relevance and quality'],
    followUpQuestions: ['What is learning to rank?', 'How do you handle query ambiguity?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q032',
    question: 'Design a content moderation system for social media.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Meta',
    topics: ['Content Moderation', 'Classification', 'System Design'],
    answer: 'Components: 1) Detection: text (hate speech, spam), image (NSFW, violence), video, 2) Models: multi-modal transformers, ensemble, 3) Pipeline: automated filtering → human review queue, 4) Features: content, user history, context, 5) Actions: remove, flag, shadow ban. Challenges: multilingual, context-dependent, adversarial attacks. Architecture: real-time + batch processing, appeal system, continuous learning from human feedback.',
    hints: ['Consider multiple modalities', 'Think about human-in-the-loop'],
    followUpQuestions: ['How do you handle false positives?', 'How do you detect adversarial content?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q033',
    question: 'Implement attention mechanism from scratch.',
    type: 'Coding',
    difficulty: 'Hard',
    company: 'OpenAI',
    topics: ['Transformers', 'Attention', 'Python'],
    answer: 'def scaled_dot_product_attention(Q, K, V, mask=None):\n    d_k = Q.shape[-1]\n    scores = np.matmul(Q, K.transpose(-2, -1)) / np.sqrt(d_k)\n    if mask is not None:\n        scores = scores.masked_fill(mask == 0, -1e9)\n    attention_weights = softmax(scores, axis=-1)\n    output = np.matmul(attention_weights, V)\n    return output, attention_weights\n\ndef multi_head_attention(Q, K, V, num_heads):\n    # Split into heads, apply attention, concatenate\n    pass',
    hints: ['Remember Q, K, V matrices', 'Think about scaling factor'],
    followUpQuestions: ['Why scale by sqrt(d_k)?', 'What is multi-head attention?'],
    timeToSolve: '30-40 minutes',
  },
  {
    id: 'q034',
    question: 'Implement gradient descent with momentum.',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'DeepMind',
    topics: ['Optimization', 'Algorithms', 'Python'],
    answer: 'def gradient_descent_momentum(params, grads, velocity, lr=0.01, momentum=0.9):\n    for i in range(len(params)):\n        velocity[i] = momentum * velocity[i] - lr * grads[i]\n        params[i] += velocity[i]\n    return params, velocity\n\n# Usage:\nvelocity = [np.zeros_like(p) for p in params]\nfor epoch in range(num_epochs):\n    grads = compute_gradients(params)\n    params, velocity = gradient_descent_momentum(params, grads, velocity)',
    hints: ['Think about velocity accumulation', 'Consider momentum coefficient'],
    followUpQuestions: ['What is Nesterov momentum?', 'How does momentum help?'],
    timeToSolve: '20 minutes',
  },
  {
    id: 'q035',
    question: 'Design a personalized news feed ranking system.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Twitter',
    topics: ['Ranking', 'Personalization', 'System Design'],
    answer: 'Components: 1) Candidate generation: following, trending, interests, 2) Ranking model: two-tower (user, content), 3) Features: engagement history, content quality, recency, diversity, 4) Objectives: engagement, time spent, satisfaction, 5) Serving: real-time scoring, caching. Architecture: online + offline components, A/B testing framework. Challenges: filter bubble, engagement vs quality, real-time updates, scalability.',
    hints: ['Think about personalization vs discovery', 'Consider multiple objectives'],
    followUpQuestions: ['How do you balance engagement and quality?', 'How do you handle cold start users?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q036',
    question: 'Implement a simple neural network forward pass.',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'Meta',
    topics: ['Neural Networks', 'Python'],
    answer: 'def relu(x):\n    return np.maximum(0, x)\n\ndef forward_pass(X, W1, b1, W2, b2):\n    # Layer 1\n    z1 = np.dot(X, W1) + b1\n    a1 = relu(z1)\n    \n    # Layer 2 (output)\n    z2 = np.dot(a1, W2) + b2\n    a2 = softmax(z2)\n    \n    return a2, (z1, a1, z2)  # Return activations for backprop',
    hints: ['Think about matrix multiplication', 'Remember activation functions'],
    followUpQuestions: ['How would you add batch normalization?', 'Implement backward pass?'],
    timeToSolve: '20-25 minutes',
  },
  {
    id: 'q037',
    question: 'Design an image search system using deep learning.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Pinterest',
    topics: ['Computer Vision', 'Search', 'System Design'],
    answer: 'Components: 1) Image encoding: CNN (ResNet, EfficientNet) → embeddings, 2) Index: vector database (FAISS, Pinecone), 3) Query: text-to-image (CLIP) or image-to-image, 4) Ranking: similarity + quality + engagement, 5) Serving: approximate nearest neighbor search. Architecture: offline indexing, online query processing (<200ms). Features: visual similarity, metadata, user preferences. Challenges: scalability (billions of images), multi-modal search, quality filtering.',
    hints: ['Think about embeddings and similarity search', 'Consider multi-modal capabilities'],
    followUpQuestions: ['What is CLIP?', 'How do you handle billions of images?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q038',
    question: 'Implement non-maximum suppression (NMS) for object detection.',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'Tesla',
    topics: ['Computer Vision', 'Object Detection', 'Python'],
    answer: 'def nms(boxes, scores, iou_threshold=0.5):\n    # boxes: [[x1, y1, x2, y2], ...]\n    indices = np.argsort(scores)[::-1]\n    keep = []\n    \n    while len(indices) > 0:\n        current = indices[0]\n        keep.append(current)\n        \n        if len(indices) == 1:\n            break\n            \n        ious = compute_iou(boxes[current], boxes[indices[1:]])\n        indices = indices[1:][ious < iou_threshold]\n    \n    return keep',
    hints: ['Think about IoU calculation', 'Consider sorting by confidence'],
    followUpQuestions: ['What is soft-NMS?', 'How do you compute IoU?'],
    timeToSolve: '25-30 minutes',
  },
  {
    id: 'q039',
    question: 'Design a chatbot system with intent classification and entity extraction.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Amazon',
    topics: ['NLP', 'Chatbots', 'System Design'],
    answer: 'Components: 1) NLU: intent classification (BERT), entity extraction (NER), 2) Dialog management: state tracking, policy, 3) NLG: response generation (templates or LLM), 4) Knowledge base: FAQ, product info, 5) Serving: low latency, context management. Architecture: pipeline (NLU → DM → NLG) or end-to-end (GPT). Features: conversation history, user profile, context. Challenges: multi-turn conversations, ambiguity, error handling, personalization.',
    hints: ['Think about conversation flow', 'Consider both rule-based and ML approaches'],
    followUpQuestions: ['How do you handle multi-turn conversations?', 'What is dialog state tracking?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q040',
    question: 'Implement beam search for sequence generation.',
    type: 'Coding',
    difficulty: 'Hard',
    company: 'Google',
    topics: ['NLP', 'Generation', 'Algorithms'],
    answer: 'def beam_search(model, start_token, beam_width=3, max_length=20):\n    sequences = [[start_token]]\n    scores = [0.0]\n    \n    for _ in range(max_length):\n        candidates = []\n        for seq, score in zip(sequences, scores):\n            if seq[-1] == end_token:\n                candidates.append((seq, score))\n                continue\n            \n            probs = model.predict(seq)\n            top_k = np.argsort(probs)[-beam_width:]\n            \n            for token in top_k:\n                new_seq = seq + [token]\n                new_score = score + np.log(probs[token])\n                candidates.append((new_seq, new_score))\n        \n        # Keep top beam_width\n        candidates.sort(key=lambda x: x[1], reverse=True)\n        sequences = [c[0] for c in candidates[:beam_width]]\n        scores = [c[1] for c in candidates[:beam_width]]\n    \n    return sequences[0]',
    hints: ['Think about maintaining top-k sequences', 'Consider log probabilities'],
    followUpQuestions: ['What is the difference from greedy search?', 'What are limitations of beam search?'],
    timeToSolve: '30-40 minutes',
  },
  {
    id: 'q041',
    question: 'Design a video recommendation system like YouTube.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'YouTube',
    topics: ['Recommendation Systems', 'Video', 'System Design'],
    answer: 'Components: 1) Candidate generation: collaborative filtering, content-based, 2) Ranking: deep neural network with features (watch history, video features, context), 3) Features: user engagement, video quality, freshness, diversity, 4) Objectives: watch time, satisfaction, 5) Serving: real-time inference, caching. Architecture: two-stage (retrieval + ranking), A/B testing. Challenges: cold start, scalability (billions of videos), real-time updates, filter bubble.',
    hints: ['Think about two-stage architecture', 'Consider watch time as primary metric'],
    followUpQuestions: ['How do you handle new videos?', 'How do you ensure diversity?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q042',
    question: 'Implement a simple autoencoder in PyTorch.',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'NVIDIA',
    topics: ['Autoencoders', 'PyTorch', 'Deep Learning'],
    answer: 'import torch.nn as nn\n\nclass Autoencoder(nn.Module):\n    def __init__(self, input_dim, latent_dim):\n        super().__init__()\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, 128),\n            nn.ReLU(),\n            nn.Linear(128, latent_dim)\n        )\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, 128),\n            nn.ReLU(),\n            nn.Linear(128, input_dim),\n            nn.Sigmoid()\n        )\n    \n    def forward(self, x):\n        latent = self.encoder(x)\n        reconstructed = self.decoder(latent)\n        return reconstructed',
    hints: ['Think about encoder-decoder structure', 'Consider activation functions'],
    followUpQuestions: ['What is a variational autoencoder?', 'How do you train an autoencoder?'],
    timeToSolve: '20-25 minutes',
  },
  {
    id: 'q043',
    question: 'Design a spam detection system for emails.',
    type: 'System Design',
    difficulty: 'Medium',
    company: 'Gmail',
    topics: ['Classification', 'NLP', 'System Design'],
    answer: 'Components: 1) Feature extraction: text (TF-IDF, embeddings), metadata (sender, links), 2) Models: ensemble (Naive Bayes, neural networks), 3) Rules: blacklist, regex patterns, 4) Feedback: user reports, 5) Serving: real-time classification. Architecture: multi-layer (rules → ML → user feedback). Features: content, sender reputation, links, attachments. Challenges: adversarial attacks, false positives, multilingual, evolving spam patterns.',
    hints: ['Think about combining rules and ML', 'Consider user feedback loop'],
    followUpQuestions: ['How do you handle adversarial spam?', 'What metrics do you optimize?'],
    timeToSolve: '30-40 minutes',
  },
  {
    id: 'q044',
    question: 'Implement a confusion matrix calculator.',
    type: 'Coding',
    difficulty: 'Easy',
    company: 'Meta',
    topics: ['Evaluation', 'Metrics', 'Python'],
    answer: 'def confusion_matrix(y_true, y_pred, num_classes):\n    matrix = np.zeros((num_classes, num_classes), dtype=int)\n    for true, pred in zip(y_true, y_pred):\n        matrix[true][pred] += 1\n    return matrix\n\ndef metrics_from_confusion_matrix(cm):\n    # For binary classification\n    tn, fp, fn, tp = cm[0][0], cm[0][1], cm[1][0], cm[1][1]\n    precision = tp / (tp + fp) if (tp + fp) > 0 else 0\n    recall = tp / (tp + fn) if (tp + fn) > 0 else 0\n    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0\n    accuracy = (tp + tn) / (tp + tn + fp + fn)\n    return {\'precision\': precision, \'recall\': recall, \'f1\': f1, \'accuracy\': accuracy}',
    hints: ['Think about true/false positives/negatives', 'Consider multi-class case'],
    followUpQuestions: ['How do you handle multi-class?', 'What is macro vs micro averaging?'],
    timeToSolve: '15-20 minutes',
  },
  {
    id: 'q045',
    question: 'Design a real-time translation system.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Google Translate',
    topics: ['NLP', 'Translation', 'System Design'],
    answer: 'Components: 1) Model: transformer (encoder-decoder), 2) Languages: multilingual model or language pairs, 3) Features: context, formality, domain, 4) Serving: low latency (<500ms), caching, 5) Quality: BLEU score, human evaluation. Architecture: model serving (TensorFlow Serving), load balancing, caching frequent translations. Challenges: rare languages, context understanding, idioms, real-time constraints, quality vs speed.',
    hints: ['Think about transformer architecture', 'Consider latency requirements'],
    followUpQuestions: ['How do you handle rare languages?', 'What is back-translation?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q046',
    question: 'Implement learning rate scheduling (cosine annealing).',
    type: 'Coding',
    difficulty: 'Medium',
    company: 'OpenAI',
    topics: ['Optimization', 'Training', 'Python'],
    answer: 'import math\n\ndef cosine_annealing_lr(epoch, total_epochs, lr_max, lr_min=0):\n    return lr_min + (lr_max - lr_min) * (1 + math.cos(math.pi * epoch / total_epochs)) / 2\n\ndef cosine_annealing_with_warmup(epoch, warmup_epochs, total_epochs, lr_max, lr_min=0):\n    if epoch < warmup_epochs:\n        return lr_max * epoch / warmup_epochs\n    else:\n        return cosine_annealing_lr(epoch - warmup_epochs, total_epochs - warmup_epochs, lr_max, lr_min)',
    hints: ['Think about cosine function', 'Consider warmup phase'],
    followUpQuestions: ['Why use cosine annealing?', 'What is learning rate warmup?'],
    timeToSolve: '20 minutes',
  },
  {
    id: 'q047',
    question: 'Design a voice assistant system like Alexa.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Amazon',
    topics: ['Speech', 'NLP', 'System Design'],
    answer: 'Components: 1) Wake word detection: small model on device, 2) ASR: speech-to-text (Whisper, Conformer), 3) NLU: intent + entities, 4) Dialog management, 5) TTS: text-to-speech, 6) Skills: extensible actions. Architecture: edge (wake word) + cloud (ASR, NLU, TTS), streaming audio. Challenges: noise robustness, low latency, privacy, multi-turn conversations, personalization, multiple languages.',
    hints: ['Think about edge vs cloud processing', 'Consider latency and privacy'],
    followUpQuestions: ['How do you handle background noise?', 'What is wake word detection?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q048',
    question: 'Implement data augmentation for images.',
    type: 'Coding',
    difficulty: 'Easy',
    company: 'Tesla',
    topics: ['Computer Vision', 'Data Augmentation', 'Python'],
    answer: 'import cv2\nimport numpy as np\n\ndef augment_image(image):\n    # Random horizontal flip\n    if np.random.rand() > 0.5:\n        image = cv2.flip(image, 1)\n    \n    # Random rotation\n    angle = np.random.uniform(-15, 15)\n    h, w = image.shape[:2]\n    M = cv2.getRotationMatrix2D((w/2, h/2), angle, 1.0)\n    image = cv2.warpAffine(image, M, (w, h))\n    \n    # Random brightness\n    value = np.random.uniform(0.8, 1.2)\n    image = np.clip(image * value, 0, 255).astype(np.uint8)\n    \n    # Random crop\n    crop_size = int(min(h, w) * 0.9)\n    x = np.random.randint(0, w - crop_size)\n    y = np.random.randint(0, h - crop_size)\n    image = image[y:y+crop_size, x:x+crop_size]\n    \n    return image',
    hints: ['Think about common transformations', 'Consider randomness'],
    followUpQuestions: ['What is mixup augmentation?', 'How do you choose augmentations?'],
    timeToSolve: '20-25 minutes',
  },
  {
    id: 'q049',
    question: 'Design an anomaly detection system for time series data.',
    type: 'System Design',
    difficulty: 'Hard',
    company: 'Datadog',
    topics: ['Anomaly Detection', 'Time Series', 'System Design'],
    answer: 'Components: 1) Data ingestion: streaming time series, 2) Models: statistical (ARIMA, Prophet), ML (Isolation Forest, Autoencoder), 3) Features: seasonality, trend, lag features, 4) Detection: threshold-based, ML-based, 5) Alerting: severity, deduplication. Architecture: real-time processing, sliding windows, model retraining. Challenges: concept drift, seasonality, false positives, scalability, interpretability.',
    hints: ['Think about time series characteristics', 'Consider both statistical and ML approaches'],
    followUpQuestions: ['How do you handle seasonality?', 'What is concept drift?'],
    timeToSolve: '40-50 minutes',
  },
  {
    id: 'q050',
    question: 'Implement a simple GAN (Generative Adversarial Network).',
    type: 'Coding',
    difficulty: 'Hard',
    company: 'NVIDIA',
    topics: ['GANs', 'Generative Models', 'PyTorch'],
    answer: 'import torch.nn as nn\n\nclass Generator(nn.Module):\n    def __init__(self, latent_dim, output_dim):\n        super().__init__()\n        self.model = nn.Sequential(\n            nn.Linear(latent_dim, 128),\n            nn.ReLU(),\n            nn.Linear(128, 256),\n            nn.ReLU(),\n            nn.Linear(256, output_dim),\n            nn.Tanh()\n        )\n    \n    def forward(self, z):\n        return self.model(z)\n\nclass Discriminator(nn.Module):\n    def __init__(self, input_dim):\n        super().__init__()\n        self.model = nn.Sequential(\n            nn.Linear(input_dim, 256),\n            nn.LeakyReLU(0.2),\n            nn.Linear(256, 128),\n            nn.LeakyReLU(0.2),\n            nn.Linear(128, 1),\n            nn.Sigmoid()\n        )\n    \n    def forward(self, x):\n        return self.model(x)',
    hints: ['Think about generator and discriminator', 'Consider adversarial training'],
    followUpQuestions: ['How do you train a GAN?', 'What is mode collapse?'],
    timeToSolve: '30-40 minutes',
  },
];
